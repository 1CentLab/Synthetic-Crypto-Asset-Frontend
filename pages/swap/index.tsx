import React from 'react';
import TradeComponent from '../../components/pages/Trade';
import { useBalance } from '../../hooks/useBalance';

type Props = {};

const TradeApp = (props: Props) => {
  return <TradeComponent />;
};

export default TradeApp;
